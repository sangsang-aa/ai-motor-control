import React from 'react'
import ReactDOM from 'react-dom/client'
import MotorWindow from './MotorWindow'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MotorWindow />
  </React.StrictMode>
)
