# 可选:服务端 LLM 代理默认配置(仅当用户在"设置"面板未配置时才生效)。
# 推荐方式:直接在应用内的"设置"面板填入 AI 供应商/Base URL/API Key/模型(存 localStorage),无需本文件。
# 若要用服务端默认(如生产部署不想暴露 key 给前端),复制为 .env.local 并填真实值。

LLM_BASE_URL="https://dashscope.aliyuncs.com/compatible-mode/v1"
LLM_API_KEY=""
LLM_MODEL="qwen-plus"
